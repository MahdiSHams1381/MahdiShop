@using Microsoft.AspNetCore.Http;
@using Microsoft.AspNetCore.Mvc;
@using System;
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MahdiShop</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    @if (User.Identity.IsAuthenticated)
    {
        <div onclick="closeModalBuy()" id="modal-backdrop" class="modal-backdrop" style="padding: 4%; opacity : 0 ;background-color: #476576f2;display: flex;height: 61%;width: 50%;position: fixed;z-index: 1000;top: 21%;left: 25%;border-radius: 64px;box-shadow: rgba(50, 50, 93, 0.25) 0px 30px 60px -12px inset, rgba(0, 0, 0, 0.3) 0px 18px 36px -18px inset;">
            
    </div>
    }
    @* <header>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container">
    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">MahdiShop</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
    <ul class="navbar-nav flex-grow-1">
    <li class="nav-item">
    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
    </li>
    <li class="nav-item">
    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
    </li>
    </ul>
    </div>
    </div>
    </nav>
    </header>*@
    <!-- ======= Header ======= -->
    <header id="header" class="fixed-top d-flex align-items-center header-transparent">
        <div class="container d-flex justify-content-between align-items-center" style="background-color:">

            <div class="logo">
                @if (User.Identity.IsAuthenticated)
                {
                    <p class="text-light"><span>wellcom @User.Identity.Name to MahdiShop</span></p>

                }
                else
                {

                    <p class="text-light"><a class="text-light" href="SignIn"><span class="text-light">Login</span></a></p>
                }
                <!-- Uncomment below if you prefer to use an image logo -->
                <!-- <a href="index.html"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->
            </div>


        </div>
    </header><!-- End Header -->
        <div class="row">
            <main role="main" class="col-md-12">
                @RenderBody()
            </main>
       
            44<div id="menue">
                <partial name="menue"></partial>
            </div>
        </div>


    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2023 - MahdiShop - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
<script>
    function openModalBuy(){
        console.log("ssssssss");
        var text = "" ;
        var tag = document.createElement("p");
        for (var q = 0; q < document.cookie.split(';').length; q++) {
            var w = document.cookie.split(';') ;
            var r = w[q].split('=') ;
        var text = document.createTextNode(r[1])
        tag.appendChild(text);
          var tag2 = document.createElement("br");
            tag.appendChild(tag2)
            text += r[1]
        }
        document.getElementById("modal-backdrop").appendChild(tag);
        document.getElementById("modal-backdrop").style.opacity = 100;  
    }
    function closeModalBuy(){
        document.getElementById("modal-backdrop").style.opacity = 0;
        console.log("dddddddddddddddddddddd");
    }
</script>